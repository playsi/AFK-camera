package org.playsi.afkcam.client.Camera;

import org.playsi.afkcam.client.RPsParser.CinematicCameraResourceReloadListener;
import org.playsi.afkcam.client.Utils.CameraAnimation;

import java.util.List;

public class CameraInterpolation {

    private final float iff (int d){
        List<CameraAnimation> AnimList = CinematicCameraResourceReloadListener.getCachedAnimations();
        return d;
    }
}
